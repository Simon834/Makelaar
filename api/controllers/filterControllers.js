const { Property } = require("../db");

async function filterProperties(req, res, next){
    { name,
      area,
      rooms,
      bathrooms,
      types,
      city,
      neighborhood
    } = req.

}